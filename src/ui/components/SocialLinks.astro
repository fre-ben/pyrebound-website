---
import { Icon } from 'astro-icon/components';

interface SocialLink {
  label: string;
  href: string;
  icon: string;
}

interface Props {
  links?: SocialLink[];
}

const {
  links = [
    { label: 'LinkedIn', href: '#', icon: 'pixel:linkedin' },
    { label: 'YouTube', href: '#', icon: 'pixel:youtube' },
    { label: 'Instagram', href: '#', icon: 'pixel:instagram' },
    { label: 'Bluesky', href: '#', icon: 'pixel:bluesky' },
  ],
} = Astro.props;
---

<section id="socials" class="w-full px-2 pb-6 sm:px-30">
  <ul class="flex items-center justify-between">
    {
      links.map((link) => (
        <li>
          <a
            href={link.href}
            aria-label={link.label}
            class="text-foreground hover:text-foreground/70 dark:text-foreground dark:hover:text-foreground/70 transition-colors duration-150"
            target="_blank"
            rel="noopener noreferrer"
          >
            <Icon
              name={link.icon}
              size={48}
              class="w-10 h-10 sm:w-12 sm:h-12 hover:scale-110 motion-reduce:hover:scale-100"
            />
          </a>
        </li>
      ))
    }
  </ul>
</section>
